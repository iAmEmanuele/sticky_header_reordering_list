name: sticky_header_reordering_list
description: A Flutter plugin for creating reordering lists with sticky headers and grouped sections.
version: 1.0.1
homepage: https://github.com/iAmEmanuele/sticky_header_reordering_list

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec



